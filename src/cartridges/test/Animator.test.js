import { Animator } from '../AnimationSequencer.js'

describe('Testing Animator from AnimationSequencer', () => {
    it('Should modify background with 2 animation modifiers if array of 2 animators is given', () => {
        
    })
})